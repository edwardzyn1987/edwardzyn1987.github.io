# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# superpowers
.superpowers/

# video staging area (Edward's local upload buffer — copied into public/videos/ as needed)
隐恕视频/

# recitation videos — served from separate repo edwardzyn1987/poetry-videos via jsDelivr CDN.
# Kept on disk locally for `npm run dev` preview, but excluded from git + the deployed dist/
# (GitHub Pages 1 GB limit). Generate HLS with scripts/segment_videos.py, then push the mp4 +
# HLS folder to the poetry-videos repo — NOT here.
public/videos/

# python cache + temp scratch
scripts/__pycache__/
temp/

# one-off re-encode target lists for segment_videos.py --only (scratch, keep local)
.reencode_targets.txt
